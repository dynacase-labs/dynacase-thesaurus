#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi

$wpub/wsh.php --api=migr_histo

$wpub/wsh.php --api=migr_2.3.1 | psql $dbfree > /dev/null
echo "update fld set qtype=qtype;"  | psql $dbfree
