#%PAM-1.0
# To use the authpam authentication module with pam_authent, you must
# configure your PAM library to authenticate the "pam_authent" service.

# Comment this line for a
auth    required        /lib/security/pam_what.so dbname=anakeen dbuser=anakeen
account required        /lib/security/pam_what.so dbname=anakeen dbuser=anakeen
