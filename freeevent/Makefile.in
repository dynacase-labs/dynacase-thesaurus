# ============================================
# $Id: Makefile.in,v 1.3 2007/07/06 12:18:10 yannick Exp $
# ============================================
PACKAGE = @PACKAGE@
VERSION = @VERSION@
utildir=@PUBRULE@
appname = @PACKAGE@
pubdir = @prefix@
srcdir = @srcdir@

export pubdir utildir appname

TAR = gtar
GZIP_ENV = --best

export targetdir PACKAGE

SUBDIR=  Class Images Zone debian

include $(utildir)/PubRule

DISTFILES += $(SUBDIR) \
            RELEASE VERSION \
	freeevent.ods	    

$(pubdir)/API:
	mkdir -p $@


$(pubdir)/EXTERNALS :
	mkdir -p $@

$(pubdir)/EXTERNALS/event.php: $(pubdir)/EXTERNALS 
	cp $(appname)_external.php $(pubdir)/EXTERNALS/event.php

$(pubdir)/$(appname)/freeevent.ods : freeevent.ods 
	cp freeevent.ods $(pubdir)/$(appname)

publish: $(pubdir)/$(appname)/freeevent.ods $(pubdir)/EXTERNALS/event.php
