# ============================================
# $Id: Makefile.in,v 1.7 2008/03/28 13:06:07 eric Exp $
# ============================================
PACKAGE = @PACKAGE@
VERSION = @VERSION@
utildir = @PUBRULE@
appname = @APPNAME@
pubdir = @prefix@
srcdir = @srcdir@

export pubdir utildir appname

TAR = gtar
GZIP_ENV = --best

export targetdir PACKAGE

SUBDIR= Class Wsdl Action Tools Xml

include $(utildir)/PubRule

DISTFILES += $(SUBDIR) \
            RELEASE VERSION  	    

$(pubdir)/$(appname):
	mkdir -p $(pubdir)/$(appname)

$(pubdir)/$(appname)/apache_ws.conf: apache_ws.conf $(pubdir)/$(appname)
	cp $< $(pubdir)/$(appname)


publish: $(pubdir)/$(appname)/apache_ws.conf $(pubdir)/$(appname)
