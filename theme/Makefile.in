# ============================================
# $Id: Makefile.in,v 1.3 2007/10/22 13:36:25 yannick Exp $
# ============================================
PACKAGE = @PACKAGE@
VERSION = @VERSION@
utildir=@PUBRULE@
appname = WHAT
pubdir = @prefix@
srcdir = @srcdir@

export pubdir utildir appname

TAR = gtar
GZIP_ENV = --best

export targetdir PACKAGE

SUBDIR= Style debian
TOP_MODULES =  wtheme

include $(utildir)/PubRule

DISTFILES += $(SUBDIR) wtheme \
            RELEASE VERSION 

$(pubdir):
	mkdir -p $(pubdir)
$(pubdir)/wtheme: wtheme $(pubdir)
	cp $< $@

